<%= form_with(model: group_cloth) do |form| %>
  <% if group_cloth.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(group_cloth.errors.count, "error") %> prohibited this group_cloth from being saved:</h2>

      <ul>
        <% group_cloth.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :cloth_id, style: "display: block" %>
    <%= form.text_field :cloth_id %>
  </div>

  <div>
    <%= form.label :group_id, style: "display: block" %>
    <%= form.text_field :group_id %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
